#pragma once

#include <vector>

#include <base/base.hpp>

namespace Omega
{
    namespace Widget
    {
        namespace Input
        {
            void process(std::vector<Base*> widgets);
        } // namespace Input
    } // namespace Widget
} // namespace Omega